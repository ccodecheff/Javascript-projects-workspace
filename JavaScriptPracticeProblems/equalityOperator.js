// Note: '==' and '===' operator difference
// 0 == "";        // true becoz it converts to the matching type before comparison
// 1 == "1";       // true
// 1 == true;      // true

// 0 === "";       // false becoz it doesn't converts to the matching type before comparison
// 1 === "1";      // false
// 1 === true;     // false